services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: medienscouts
      POSTGRES_USER: medienscouts
      POSTGRES_PASSWORD: medienscouts
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  web:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg2://medienscouts:medienscouts@postgres:5432/medienscouts
      RATELIMIT_STORAGE_URI: redis://redis:6379/1
      UPLOAD_FOLDER: static/uploads
      USER_PROFILES: static/uploads/profiles
    ports:
      - "8000:8000"
    volumes:
      - app_uploads:/app/app/static/uploads
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  app_uploads:
